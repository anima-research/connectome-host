- Makeup panel: the exact `count_tokens` figure now counts the request the agent's own formatter would send — built through `buildMessages` (participant prefixes per text block, tool-pair normalization, same-role merging, native tools) so signed `thinking` / `redacted_thinking`, `tool_use`, `tool_result` and images all count — instead of a text-only flattening that omitted most of a keep-all model's prompt (read up to ~10× below billed). The response also carries `lastBilledInputTokens`: the provider's fresh + cache-read + cache-creation total of the agent's last call, a free exact reading that needs no request.

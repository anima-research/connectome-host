- Recipe `agent.strategy.primaryToolProseHoist` — same shape and the same loud
  validation as `compressionToolProseFallback`, passed through to Context
  Manager's primary render hoist (long `skip_reply.reason` / `think.content`
  arguments rendered as calls to the agent's real `journal` tool on every
  primary compile).
